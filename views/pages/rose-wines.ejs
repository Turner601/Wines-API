<%- include('../partials/header'); %>

<main class="main main-rose container text-center">
    <h2 class="main-h2">
        <%= name %>
    </h2>
    <section class="section section-rose">
        <div class="row">
            <% data.forEach(item => { %>
                <div class="col-md-3">
                    <figure class="figure figure-rose">
                        <a href="/wines/rose/<%= item.id %>">
                            <img src="<%= item.image %>" alt="<%= item.wine %>" class="img-fluid wine-image">
                            <figcaption class="figcation figcap-rose">
                                <%= item.wine %>
                            </figcaption>
                        </a>
                    </figure>
                </div>
            <% }) %>
        </div>
    </section>
</main>

<%- include('../partials/footer'); %>