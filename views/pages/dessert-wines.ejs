<%- include('../partials/header'); %>

<main class="main main-dessert container text-center">
    <h2 class="main-h2">
        <%= name %>
    </h2>
    <section class="section section-dessert">
        <div class="row">
            <% data.forEach(item => { %>
                <div class="col-md-3">
                    <figure class="figure figure-dessert">
                        <a href="/wines/dessert/<%= item.id %>">
                            <img src="<%= item.image %>" alt="<%= item.wine %>" class="img-fluid wine-image">
                            <figcaption class="figcation figcap-dessert">
                                <%= item.wine %>
                            </figcaption>
                        </a>
                    </figure>
                </div>
            <% }) %>
        </div>
    </section>
</main>

<%- include('../partials/footer'); %>